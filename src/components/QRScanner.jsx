import React, { useState } from "react";
import axios from "axios";

const QRScanner = () => {

  return (
    <div>
      <h1>QR Code Scanner</h1>
      <p>Scan the QR code to get the data.</p>
    </div>
  );
};

export default QRScanner;
